{{- $git_user := promptString "git_user" -}}
{{- $git_email := promptString "git_email" -}}

{{- $isWSL := eq .chezmoi.username "wsl" }}
{{- $isMac := eq .chezmoi.os "darwin" }}
{{- $isLinux := eq .chezmoi.os "linux" }}
{{- $isLocal := or $isMac $isWSL }}
{{- $isWork := eq .chezmoi.username $git_user }}

{{- $win_user := "" -}}

{{- if $isWSL }}
    {{- $win_user = promptString "win_user" -}}
{{- end -}}

[data]
    isWSL = {{ $isWSL }}
    isLinux = {{ $isLinux }}
    isMac = {{ $isMac }}
    isLocal = {{ $isLocal }}
    isWork = {{ $isWork }}

    git_user = "{{ $git_user }}"
    git_email = "{{ $git_email }}"

{{- if $isWSL }}
    win_user = "{{ $win_user }}"
{{- end }}
